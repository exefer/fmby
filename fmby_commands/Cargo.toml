[package]
name = "fmby_commands"
version = "0.1.0"
edition = "2024"

[dependencies]
fmby_core = { path = "../fmby_core" }
fmby_entities = { path = "../fmby_entities" }
poise.workspace = true
sea-orm.workspace = true
