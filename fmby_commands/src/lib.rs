pub use fmby_core::{
    error::Error,
    structs::{Command, Context, Data},
};
#[must_use]
pub fn commands() -> Vec<crate::Command> {
    let commands: Vec<crate::Command> = vec![];

    commands
}
